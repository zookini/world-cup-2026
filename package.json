{
  "name": "world-cup-2026",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "verify:smoke": "playwright test --grep \"mock mode loads and stays offline|tabs render and route|all images decode and no asset request fails\"",
    "verify": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "wrangler": "^4.99.0"
  }
}
